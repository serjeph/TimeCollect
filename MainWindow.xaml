<Window x:Class="TimeCollect.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeCollect"
        mc:Ignorable="d"
        Title="TimeCollect" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl>
            <TabItem Header="Data">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="employeesDataGrid" Grid.Row="0" AutoGenerateColumns="False">
                        
                    </DataGrid>

                    <Button x:Name="runButton" Grid.Row="1" Content="Run"/>

                    <ProgressBar x:Name="progressBar" Grid.Row="2"/>

                    <TextBox x:Name="logTextBox" Grid.Row="3" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </TabItem>
            <TabItem Header="Settings">
                <StackPanel Orientation="Vertical" Margin="10">
                    <TextBlock Text="Client ID:"/>
                    <TextBox x:Name="clientIdTextBox" Margin="5" Text="{Binding ClientId, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="Client Secret:" />
                    <TextBox x:Name="clientSecretTextBox" Margin="5" Text="{Binding ClientSecret, UpdateSourceTrigger=PropertyChanged}"/>

                    <Button x:Name="saveCredentialsButton" Content="Save Credentials" Margin="5" />
                </StackPanel>
            </TabItem>
            <TabItem Header="Sheet Names">
                <TextBox x:Name="sheetNamesTextBox"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
